
CREATE TABLE CONTACT(ID INT PRIMARY KEY AUTO_INCREMENT NOT NULL, FIRST_NAME VARCHAR(30) NOT NULL, LAST_NAME VARCHAR(30) NOT NULL, PHONE_NUMBER INT(10) NOT NULL, ADDRESS VARCHAR(100) NOT NULL, EMAIL_ADDRESS VARCHAR(30) NOT NULL);